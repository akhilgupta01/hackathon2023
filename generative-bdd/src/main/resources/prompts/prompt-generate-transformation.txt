Generate a Java function to transform data records
of type ${input-data-record-type} with attributes ${input-data-record-attributes}
to data records of type ${output-data-record-type} with attributes ${output-data-record-attributes}
as described in the scenario delimited by triple backticks.
```${scenario_text}```
Also, use following instructions to provide the response:
- Output should have methodName and methodBody in JSON format
- Method should not be a static method
- Use Java 8 features