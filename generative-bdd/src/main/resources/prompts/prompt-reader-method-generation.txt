A Java class represented in JSON format (with attributes className, classImports, methodSignatures, classBody and mavenDependencies)
to read Java objects from a ${input-data-source-type}
Using following assumptions
- using Java 8 features
- classBody should be represented as a single line
- maven dependency (if any) should be in format groupId:artifactId:version
- model entity already exist and is described by the JSON ${input-data-entity}
will be as follows: