Generate a Java class as described by the JSON object delimited by triple backticks.
```${data-entity-json}```
Follow the instructions below to provide the response:
- Do not provide any constructor or the getter and setter methods use lombok @Data annotation instead
- Provide any required maven dependencies with group, artifact and version
- Provide result as a JSON object with following attributes: className, classImports, classBody and mavenDependencies